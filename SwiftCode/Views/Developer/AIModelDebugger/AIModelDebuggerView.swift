import SwiftUI

struct AIModelDebuggerView: View {
    var body: some View {
        Form {
            Section("Prompt Testing") {
                Text("View last prompt data...")
            }
        }
        .navigationTitle("AI Model Debugger")
    }
}
